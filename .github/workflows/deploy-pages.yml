name: Deploy to GitHub Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build Wasm distribution
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Cache Kotlin/Konan and JS store
        uses: actions/cache@v5
        with:
          path: |
            ~/.konan
            kotlin-js-store
          key: ${{ runner.os }}-kotlin-${{ hashFiles('gradle/libs.versions.toml', 'gradle/wrapper/gradle-wrapper.properties', 'kotlin-js-store/**') }}
          restore-keys: |
            ${{ runner.os }}-kotlin-

      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v6

      - name: Build Wasm browser distribution
        run: ./gradlew :composeApp:wasmJsBrowserDistribution --no-daemon --stacktrace

      - name: Add SPA 404 fallback
        run: |
          set -euo pipefail
          DIST_DIR="composeApp/build/dist/wasmJs/productionExecutable"
          cp "${DIST_DIR}/index.html" "${DIST_DIR}/404.html"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: composeApp/build/dist/wasmJs/productionExecutable

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v5
